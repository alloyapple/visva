package com.visva.android.flashlight.widget;

import android.appwidget.AppWidgetManager;
import android.appwidget.AppWidgetProvider;
import android.content.Context;

public class TinyFlashWidgetProvider extends AppWidgetProvider {
	//private static final String ACTION_TOUCH = "ACTION_TOUCH";
	
	@Override
	public void onUpdate(Context context, AppWidgetManager appWidgetManager, int[] appWidgetIDs) {
		//ComponentName __widgetName = new ComponentName(context, TinyFlashWidgetProvider.class);
		/*
		int[] __listWidgetIDs = appWidgetManager.getAppWidgetIds(__widgetName);
		for(int __widgetID : __listWidgetIDs) {
			//int __randomNumber = (new Random()).nextInt(100);
			
			//RemoteViews __remoteViewer = new RemoteViews(context.getPackageName(), R.layout.activity_list_item);
			
		}
		*/
	}
}
